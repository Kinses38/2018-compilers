void func (x:integer, y:integer) is
    variable test:integer;
    variable othertest:boolean;
begin
  if ~(2 + 1 > y & x > 10)
    begin
        x := y;
        y := -y;
    end
  return(y);
end

main
begin

  variable i:integer;
  i := 1;
  func (i, i);
end
